NestLoop:
when we write a loop inside another loop, it is called a nested loop.

Example:
for(int i=10; i<=5; i++) {
for(int j=1; j<=5; j++) {
sout "I am sorry"}
}
}
Array:
Array is a collection of similar items, stored in one single variable.These variables
can be stored and accessed using indexes. An index is a number that is assigned to every item we store
in array.
Syntax:
There are two ways of creating an array: 1.create and fill at the same time,
and 2.create an empty array and use a loop or smth to fill it later.
DataType[] nameOfTheArray={item1, item2, item3..}
for storing the names
String[] names={"Sino", "Naso", "Kendi", "Nasima"};
int [] numbers={10, 12, 40};
char [] gender={'M', 'F', 'U'};
boolean [] cons={false, true, true, false};
int [] numbers2=new int[1000000];